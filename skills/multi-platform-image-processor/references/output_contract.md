# 输出契约

## 完整成品包

每个产品交付目录包含六个平台目录和三个业务图片目录：

```text
产品交付目录
├─ 天猫通用版
├─ CBME
├─ 蜂享家＋爱库存
├─ 京东
├─ 唯品会
├─ 站外通用版
├─ 合格证
│  └─ 合格证图.jpg
├─ 吊牌图
│  └─ 吊牌图.jpg
└─ 尺码图
   └─ 尺码图.jpg
```

## 输出根目录

默认输出目录包含以下平台目录：

- `天猫通用版`
- `CBME`
- `蜂享家＋爱库存`
- `京东`
- `唯品会`
- `站外通用版`

## 失败策略

全流程采用“尽量输出”策略：

- 输入包文件夹结构、透明图脏点或待确认独立区域检测失败时采用“立即停止”策略：只输出报告，不创建任何平台内容。
- 能处理但不完全确定时输出结果，并写入 `风险`。
- 能处理但不完全符合大小或数量约束时输出结果，并写入 `警告`。
- 透明 PNG 调用 `pngquant` 并验收退出码、输出文件和文件大小。工具缺失、执行异常或未生成有效结果时写入 `失败项`；有效结果仍超过平台限制时写入 `警告`。
- 文件无法打开、无法写出或关键来源缺失时写入 `失败项`，不中断整包处理。
- 站外 SKU 的商品卡片或彩色标签识别失败时直接按原图压缩输出并写入 `风险`；模型调用失败、模型输出宽高比异常或差异验收失败时自动重试一次，第二次仍失败才按原图压缩输出，并在 `风险` 中记录两次失败原因。
- 主报告保留每个平台的输出路径、图片统计和追溯文件路径。
- 每张图片的完整处理记录保存在独立的逐图明细文件中。

## 报告字段

报告文件是 UTF-8 JSON，保存到 skill 目录下的 `scripts/output/report/<输出目录名>-<时间戳>-report.json`，报告目录最多保留最近 100 份，使用中文字段：

- `处理配置`：源目录、模板目录、输出目录、平台参数、运行时间。
- `输入包检测`：是否通过、结构问题、透明图主体组成部分、待确认区域、脏点、可选目录警告和实际识别目录；检测失败时额外包含 `标准输入结构`。透明图存在待检查独立区域时包含 `透明图诊断图` 和 `透明图问题汇总`，每个问题项包含 `可视化诊断图`。Agent 必须完整展示标准结构和所有可视化结果。
- `输入包检测.透明图检测规则`：本次使用的 Skill 配置文件路径和已校验规则，便于追溯实际判定边界。
- `素材扫描`：识别到的源素材目录、图片数量、缺失目录。
- `平台结果`：每个平台的输出路径、输出图片数量、保留空目录。
- `图片统计`：图片总数及各平台、各用途的数量。
- `PNG压缩统计`：PNG 处理总数及成功、保留原图、超出限制、执行失败的数量。
- `Agent复核建议`：需要 Agent 做视觉判断的任务名称、图片路径、原因。
- `警告`：不阻断交付但需要关注的问题。
- `风险`：模型去字失败降级、低置信度或可能不完美的问题。
- `失败项`：无法完成的具体文件或步骤。
- `追溯文件`：本次处理对应的运行日志和逐图明细文件路径。
- `汇总`：平台数、图片数、Agent复核建议数、警告数、风险数、失败数。

顶层完整流程报告额外包含：

- `工作流`：运行 ID、开始时间、结束时间和完成状态。
- `产品匹配`：产品货号、产品名称、Excel 候选、选中记录和匹配结论。
- `路径`：源 UNC 路径、本地工作副本、最终输出和平台子报告。
- `面料检查`：Excel 中文原文、检查图片、差异、修改结果和复核状态。
- `BarTender导出`：候选文件、选中文件、代表颜色、代表尺码、导出图和源文件保护检查。
- `业务图片`：三张图片的目标路径、尺寸、生成状态和质检结果。
- `警告`、`风险`、`失败项`、`Agent复核建议`：沿用平台报告级别并汇总平台子报告内容。
- `汇总`：产品数、业务图片数、面料检查数、修改数和各级问题数量。

完成状态取值为 `完成`、`部分完成` 或 `失败`。存在失败项时不得标记为 `完成`；存在尚未确认的 Agent 复核项时标记为 `部分完成`。

## 运行日志

运行日志保存到 `scripts/output/logs/<运行ID>.log`，最多保留最近 100 份。日志记录任务和平台流程、每个图片处理项、外部工具调用、质检结果、异常与降级，不记录认证令牌等敏感信息。

## 逐图明细

逐图明细保存到 `scripts/output/report/<输出目录名>-<时间戳>-image-records.jsonl`，与主报告使用相同前缀。每行对应一张输出图片，包含运行 ID、产品、平台、用途、源文件、输出文件、尺寸、格式、大小、处理动作和处理结果。PNG 记录包含最终状态、尝试次数、退出码和大小验收结果。

Agent 默认读取主报告。需要定位具体图片或排查处理过程时，根据 `追溯文件` 查询逐图明细或运行日志。

## 命名约定

- 天猫、京东、CBME、唯品会详情页从 `601.jpg` 连续递增。
- 蜂享家＋爱库存详情页从 `详情图-01.jpg` 连续递增。
- 其他图片保留原文件名主干，按目标格式改扩展名。
- 文件名冲突时追加 `_2`、`_3` 等序号。
- 合格证图固定命名为 `合格证图.jpg`。
- 吊牌图固定命名为 `吊牌图.jpg`。
- 尺码图固定命名为 `尺码图.jpg`。

## 模板目录

模板目录用于两类事情：

- 复制已有平台空目录结构，确保缺素材目录仍被保留。
- 查找站外 `800白底图＋logo` 使用的 `logo3.png`。

默认使用 skill 内置 `template` 目录。模板目录中的空目录使用 `.gitkeep` 占位以便 Git 跟踪；复制模板空目录时忽略 `.gitkeep`。

模板目录不存在时，脚本仍按平台规则创建标准输出目录。

## 临时文件

站外 SKU 模型去字的同尺寸输入画布和临时生成图保存到 `scripts/output/image-without-text-tmp`，最多保留最近 100 张图片。临时图只用于 Agent 复核和问题排查，不写入最终交付目录。
