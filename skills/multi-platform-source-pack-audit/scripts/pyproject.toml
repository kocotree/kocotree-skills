[project]
name = "multi-platform-source-pack-audit"
version = "0.1.0"
description = "多平台原始数据包质检的只读辅助工具"
requires-python = ">=3.11"
dependencies = [
    "pillow>=11.3.0",
    "xlrd>=2.0.1",
]

[tool.uv]
package = false
